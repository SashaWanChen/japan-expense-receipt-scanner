# ⚠️ 本專案不使用 .env.local。
#
# 金鑰請一律設定為 Codespaces secrets：
#   https://github.com/settings/codespaces → Codespaces secrets → New secret
#   （Repository access 記得勾選本 repo，並 Rebuild container）
#
# 原因：Next.js 的 .env.local 優先權高於系統環境變數，只要這個檔案存在，
# 就會蓋掉 Codespaces secrets 注入的值。即使只留一行空白的 NOTION_TOKEN=，
# 也會讓設定好的 secret 失效，而且錯誤訊息不會告訴你是這個原因。
#
# 詳細步驟見 docs/SETUP.md。
#
# 以下僅供參考，說明程式會讀取哪些變數名稱：
#
#   GEMINI_API_KEY       Google AI Studio：https://aistudio.google.com/
#   NOTION_TOKEN         Notion integration：https://www.notion.so/my-integrations
#   NOTION_DATABASE_ID   Notion database 網址中 32 碼那段
#   APP_PASSWORD         選填，留空則不啟用密碼保護（Private port 不需要）
